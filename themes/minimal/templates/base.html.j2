<!DOCTYPE html>
<!-- base.html.j2 -->
<html>
<head>
    {% block head %}
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width">
    <title>{% block title %}{% endblock %} - {{ site_title }}</title>
    <base href="{{ base_path }}" />
    <link rel="stylesheet" type="text/css" href="css/style.css">
    <link rel="stylesheet" href="css/style-dark.css" media="none" type="text/css" id="dark-theme-stylesheet" />
    <style type="text/css" id="dark-theme-import">
    @import url('https://random.fangirling.net/pico/themes/fangirling/css/style-dark.css') screen and (prefers-color-scheme: dark); 
    </style>
    <script type="text/javascript" src="js/theme-toggle.js"></script>
    {% endblock %}
</head>

<body id="{{ slug }}">
<header>
<nav>
{% block navigation %}
<section>
    <span class="home">
        <a href="{{ base_path }}">Home</a>
    </span>
    <span class="links">
        {% if header_menu %}
        {% for link in header_menu %}
        <a href="{{ link.uri }}">{{ link.text }}</a>
        {% endfor %}
        {% endif %}
    </span>
</section>
<div class="user-theme-toggle" style="">
  <button id="theme-toggle-button" class="theme-toggle-button">
    <span class="theme-toggle-button-text theme-toggle-button-text-dark">Toggle Dark Mode</span>
    <span class="theme-toggle-button-text theme-toggle-button-text-light">Toggle Light Mode</span>
    <span class="theme-toggle-button-icon" aria-hidden="true"></span>
  </button>
</div>
</nav>
{% endblock %}
</header>

<main>
{% block content %}
{{ content }}
{% endblock %}
</main>

<footer>
  {% block footer %}
<section>
<p>&copy; {{ current_year }}</p>
{% if footer_menu %}
<p>
{% for link in footer_menu %}
<a href="{{ link.uri }}">{{ link.text }}</a>
{% endfor %}
</p>
{% endif %}
</section>
{% endblock %}
</footer>

</body>
</html>
